# questão 2 - terceira parte
.text
   addi $t0, $0, 2	# definindo o valor de 2 no registrador $t0
   addi $t1, $0, 3	# definindo o valor de 3 no registrador $t1
   
   mul $t2, $t1, $t0	# realiza a multiplicaçaõ de 2 * 3 -> j

