.text
   # definindo os valores para os registradores
   li $t1, 324		
   li $t2, 100		
   nor $t3, $t1, $t2	# realiza a operação "nor" entre os numeros em binario e manda para o resgistrador $t3 o resultado
   
   # definindo os valores para os registradores
   li $t1, 2018		
   li $t2, 1970		
   nor $t3, $t1, $t2	# realiza a operação "nor" entre os numeros em binario e manda para o resgistrador $t3 o resultado
   
   # definindo os valores para os registradores
   li $t1, 33		
   li $t2, 66		
   nor $t3, $t1, $t2	# realiza a operação "nor" entre os numeros em binario e manda para o resgistrador $t3 o resultado